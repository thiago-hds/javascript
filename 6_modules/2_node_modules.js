// 1 - EXPORTAÇÃO DE MÓDULOS
console.log('1 - EXPORTAÇÃO DE MÓDULOS');

const sum = (x, y) => x + y;
const square = x => x ** 2;

exports.mean = data => {
  return data.reduce(sum) / data.length;
};

module.exports = class Stats {
  // todo
};

module.exports = { mean, stddev };

// 2 - IMPORTAÇÃO DE MÓDULOS

// modulos built-in do node ou instalados podem ser importados com apenas o nome
// do módulo
const fs = require('fs');
const http = require('http');

// modulos proprios precisam ser importados com o caminho relativo ao módulo atual
const stats = require('./stats.js');

// podemos importar também apenas propriedades específicas com desestruturação
const { mean } = require('./stats');
