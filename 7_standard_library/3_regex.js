// REGEX

// uma expressão regular descreve um padrão textual
// em javascript um regex pode ser definido entre barras ou com o construtor RegExp
let pattern = /s$/;
pattern = new RegExp('s$');

// flags são especificadas no apos a segunda barra em literais
pattern = /s$/i;
